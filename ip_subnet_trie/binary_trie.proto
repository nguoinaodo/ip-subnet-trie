syntax = "proto3";

message BinaryTrieNode {
  bool is_end = 1;
  bool has_zero_child = 2;
  bool has_one_child = 3;
}

message BinaryTrieNodes {
    repeated BinaryTrieNode nodes = 1;  // The nodes in the trie
}